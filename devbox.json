{
  "packages": [
    "pre-commit@3.6.2"
  ],
  "shell": {
    "init_hook": [
      "pre-commit install -f",
      "yq '.repos[].hooks[].stages[]' .pre-commit-config.yaml | xargs -I type pre-commit install -f -t type"
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1"
      ]
    }
  }
}
