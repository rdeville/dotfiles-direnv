{
  "packages": ["pre-commit@3.6.2"],
  "shell": {
    "init_hook": ["pre-commit install -t commit-msg -t pre-push -t pre-commit"],
    "scripts": {
      "test": ["echo \"Error: no test specified\" && exit 1"]
    }
  }
}