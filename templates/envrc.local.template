#!/usr/bin/env bash

# Start tmux session as soon as possible to ensure environment is set within the
# session
# TMUXP VARIABLES
export TMUXP_SESSION_NAME="" # TODO SET TMUXP_SESSION_NAME !!
use_tmux

source_up_if_exists

dotenv_if_exists ".env.local"

# vim: ft=bash
